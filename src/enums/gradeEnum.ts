enum primary {
  MINE_COUNT = 10,
  COL_COUNT = 8,
  ROW_COUNT = 8,
}

enum intermediate {
  MINE_COUNT = 40,
  COL_COUNT = 16,
  ROW_COUNT = 16,
}

enum senior {
  MINE_COUNT = 99,
  COL_COUNT = 30,
  ROW_COUNT = 16,
}

export { primary, intermediate, senior };
